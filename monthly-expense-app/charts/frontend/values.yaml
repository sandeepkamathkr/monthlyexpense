replicaCount: 1

image:
  # Replace with your Docker Hub username and image name
  repository: sandeepkamathkr/monthly-expense-frontend
  tag: 1.0.5
  pullPolicy: Always

service:
  type: ClusterIP # Exposes the service outside the cluster
  port: 80